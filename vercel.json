{
  "version": 2,
  "builds": [
    {
      "src": "./src/**/*.@(js|jsx|ts|tsx)",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/.*",
      "dest": "src/server.js"
    }
  ]
}
